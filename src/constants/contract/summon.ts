const enum SummonStatus {
  PREPARING = "PREPARING",
  PAYING = "PAYING",
  SUMMONING = "SUMMONING",
  CLAIMING = "CLAIMING",
}

const enum SummonEvent {
  STATUS = "status",
}

export { SummonStatus, SummonEvent };
